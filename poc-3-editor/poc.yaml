version: "3.8"

services:
  editor:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./src:/usr/share/nginx/html:ro
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == manager
          